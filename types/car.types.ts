export interface ICar {
  id: string;
  name: string;
  consume_fuel: string;
  price_per_day: string;
  rating: number;
  image?: string;
  tank_capacity?: string;
  max_speed?: string;
  fuel_type?: string;
  created_at?: string;
  updated_at?: string;
}
